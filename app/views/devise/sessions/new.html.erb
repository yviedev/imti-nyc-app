===================== Breadcumb area start ===================== -->
<section class="breadcumb_area">
    <div class="container">
        <div class="row">
            <div class="col-xs-12">
                <div class="breadcumb_section">
                    <!-- Breadcumb page title start -->
                    <div class="page_title">
                        <h3>sign in</h3>
                    </div>
                    <!-- Breadcumb page pagination start -->
                    <div class="page_pagination">
                        <ul>
                            <li><%= link_to "Home", "/" %></li>
                            <li><i class="fa fa-angle-right" aria-hidden="true"></i></li>
                            <!-- Once we have devise controllers imported into our rails app, 
                            need to add @title = "sign-in" -->
                            <li>sign in</li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
<!-- ===================== Breadcumb area end ===================== -->

<!-- ===================== login area start ===================== -->
<section class="login_area section_padding_100">
    <div class="container">
        <div class="row">

            <div class="col-xs-12 col-md-5 col-lg-6">
                <!-- login thumb start -->
                <div class="login_thumb">
                    <%= image_tag "img/bg-pattern/courses-bg.jpg" %>
                    <!-- login thumb caption -->
                    <div class="login_thumb_caption">
                        <h3>Welcome Back!</h3>
                        <p>Continue your learning by sign in.</p>
                    </div>
                </div>
                <!-- login thumb end -->
            </div>

            <div class="col-xs-12 col-md-7 col-lg-6">
              <!-- login form start -->
              <div class="login_form">
                <!-- sign in manual form -->
                <div class="login_manual_form">
                  <h2>Sign-in</h2>

                  <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
                    <div class="form-group">
                      <i class="fa fa-user"></i>
                      <%= f.email_field :email, autofocus: true, :placeholder => "Email" %>
                    </div>

                    <div class="form-group">
                      <i class="fa fa-lock"></i>
                      <%= f.password_field :password, autocomplete: "off", :placeholder => "Password" %>
                    </div>

                    <% if devise_mapping.rememberable? -%>
                      <div class="">
                        <%= f.check_box :remember_me %>
                        <%= f.label :remember_me %>
                      </div>
                    <% end -%>

                    <div class="actions">
                      <%= f.submit "Log in", { :role => "button", :class => 'btn btn-default', :id => 'login_submit' } %>
                    </div>
                  <% end %>

                  <%= render "devise/shared/links" %>
                </div>
              </div>
            </div>
        </div>
    </div>
</section>
<!-- ===================== login area end =====================